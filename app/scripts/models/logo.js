define(function () {
    var Logo = {
        save: function (fileObject) {
            return Everlive.$.Files.create(fileObject);
        },
        
        getAll: function () {
            
        },
        
        getAllByUser: function () {
            
        }
    };

    return Logo;
});
